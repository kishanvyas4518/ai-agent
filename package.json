{
  "name": "ai-agent-monorepo",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
